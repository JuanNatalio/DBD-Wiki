const config = {
  auth0_domain_name: import.meta.env.VITE_AUTH0_DOMAIN_NAME,
  auth0_clientID: import.meta.env.VITE_AUTH0_CLIENTID,
  auth0_audience: import.meta.env.VITE_AUTH0_AUDIENCE,
};

export default config;
